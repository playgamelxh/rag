## Model Fine-Tuning（模型微调）

### 一、核心概念与分类

**模型微调**是在预训练模型基础上，使用特定任务的数据进行进一步训练，使模型适应特定场景的过程。

**微调方法分类**：

| 分类 | 代表方法 | 参数更新比例 | 显存需求 | 适用场景 |
|------|----------|--------------|----------|----------|
| **全量微调** | Full Fine-Tuning | 100% | 高（24GB+） | 数据充足、资源充裕 |
| **参数高效微调** | PEFT | <1% | 低（10GB+） | 中小团队、资源受限 |
| **指令微调** | SFT | 视方法而定 | 中/低 | 对齐人类指令、对话系统 |
| **领域续训** | Domain Adaptation | 视方法而定 | 中/低 | 特定领域适配 |

---

### 二、Full Fine-Tuning（全量微调）

**详细参考**：[README_FULL_FINETUNING.md](README_FULL_FINETUNING.md)

#### 定义
全量微调是更新模型所有参数的微调方式，是最传统的微调方法。

#### 原理
```
预训练模型(W_0) + 任务数据 → 更新所有参数 → 适配任务的模型(W_final)
```

#### 特点

| 维度 | 描述 |
|------|------|
| **参数更新** | 100% 所有参数 |
| **显存需求** | 高（7B模型约需24GB+） |
| **数据需求** | 大量标注数据 |
| **效果** | 理论上最优 |
| **训练时间** | 长 |

#### 适用场景
- 数据充足（数万级标注样本）
- 计算资源充裕（多GPU集群）
- 需要最大化模型性能

#### 局限性
- 显存占用巨大，普通GPU难以训练大模型
- 容易过拟合，需要大量数据
- 训练时间长，成本高

---

### 三、Freeze Fine-Tuning（冻结微调）

**详细参考**：[README_FREEZE_FINETUNING.md](README_FREEZE_FINETUNING.md)

#### 定义
冻结微调是指冻结模型的部分参数（通常是底层参数），仅训练剩余参数（通常是顶层参数）的微调方式。

#### 核心思想
```
预训练模型(W_0)
├── 底层参数（冻结，不更新）→ 保留通用语言知识
└── 顶层参数（训练，更新）→ 适应特定任务
```

#### 特点

| 维度 | 描述 |
|------|------|
| 参数更新 | 部分参数（通常10%~50%） |
| 显存需求 | 中（7B模型约需16GB+） |
| 数据需求 | 中等（数千至数万级） |
| 效果 | 介于全量微调与PEFT之间 |

#### 适用场景
- 数据量中等（数千至数万）
- 显存有限（16GB+）
- 需要保留模型通用能力

---

### 四、Parameter-Efficient Fine-Tuning (PEFT)（参数高效微调）

#### 定义
PEFT 是一类只更新少量模型参数即可实现有效微调的技术集合。

#### 核心思想
通过冻结预训练模型的大部分参数，仅训练少量新增参数，在保持效果的同时大幅降低资源消耗。

#### PEFT 方法对比

| 方法 | 参数更新比例 | 核心思想 | 特点 |
|------|--------------|----------|------|
| **LoRA** | <1% | 低秩矩阵分解 | 最主流，效果好，推理无开销 |
| **Adapter** | <1% | 插入小网络模块 | 灵活，可堆叠 |
| **Prefix Tuning** | <1% | 优化前缀向量 | 不修改模型结构 |
| **Prompt Tuning** | <1% | 优化软提示 | 参数最少 |
| **IA³** | <1% | 学习缩放因子 | 简洁高效 |

#### 适用场景
- 资源受限（消费级GPU）
- 多任务场景（快速切换）
- 需要微调大模型（7B/13B/70B）

---

### 五、LoRA Fine-Tuning（LoRA 微调）

#### 定义
LoRA（Low-Rank Adaptation）是目前最主流的轻量微调方案。

#### 核心原理
假设模型参数更新具有低秩性，将更新矩阵分解为两个低秩矩阵：
$ΔW = W_A × W_B$，其中 $W_A ∈ R^{(d×r)}$, $W_B ∈ R^{(r×d)}$，r 是秩，远小于模型维度 d（通常取 8~32）

#### 训练与推理机制
- **训练时**：冻结原模型权重，仅优化 W_A 和 W_B
- **推理时**：将 ΔW 与原权重合并（W_0 + W_A × W_B），不增加推理延迟

#### 关键参数

| 参数 | 作用 | 推荐范围 |
|------|------|----------|
| `r` | 秩，控制表达能力 | 8~32 |
| `lora_alpha` | 缩放因子 | 32~128 |
| `target_modules` | 目标模块 | 注意力层（q_proj、v_proj） |
| `lora_dropout` | Dropout 率 | 0.05~0.1 |
| `bias` | 偏置训练方式 | "none" |

#### 优势
- 显存高效（7B模型仅需10GB+）
- 保留预训练知识
- 多任务灵活切换
- 推理无额外开销

#### 详细参考
[README_LORA.md](README_LORA.md)

---

### 六、Instruction Fine-Tuning（指令微调）

**详细参考**：[README_INSTRUCTION_FINETUNING.md](README_INSTRUCTION_FINETUNING.md)

#### 定义
指令微调是使用指令形式的数据对模型进行微调，使模型理解并遵循人类指令。

#### 核心思想
通过"指令+输入+输出"的格式训练模型，使其能够处理各种自然语言指令。

#### 数据格式
```json
{
  "instruction": "请总结以下文章的主要内容",
  "input": "文章内容...",
  "output": "总结内容..."
}
```

#### 对话格式
```json
{
  "conversations": [
    {"from": "human", "value": "什么是机器学习？"},
    {"from": "gpt", "value": "机器学习是..."},
    {"from": "human", "value": "它有哪些应用？"},
    {"from": "gpt", "value": "主要应用包括..."}
  ]
}
```

#### 适用场景
- 对话系统
- 任务型AI助手
- 通用能力对齐

---

### 七、Supervised Fine-Tuning (SFT)（监督微调）

#### 定义
SFT 是使用标注数据进行监督学习的微调方法，是行业标准的微调流程。

#### 流程
```
预训练模型 → SFT（监督微调） → RLHF（可选） → 最终模型
```

#### 数据要求
- 高质量标注数据
- 多样化任务覆盖
- 格式统一规范

#### SFT vs 预训练

| 维度 | 预训练 | SFT |
|------|--------|-----|
| 目标 | 学习通用语言表示 | 学习特定任务能力 |
| 数据 | 无标注文本 | 标注数据 |
| 训练方式 | 自监督学习 | 监督学习 |
| 输出 | 预测下一个词 | 生成特定输出 |

#### 关键步骤
1. **数据收集与清洗**：确保数据质量
2. **数据格式化**：统一输入输出格式
3. **模型选择**：根据任务选择基础模型
4. **训练配置**：设置学习率、batch size 等
5. **评估与调优**：验证效果并调整

---

### 八、Continual Fine-Tuning / Domain Adaptation Fine-Tuning（领域续训微调）

#### 定义
领域续训是在通用预训练模型基础上，使用特定领域数据进行微调，使模型适应领域知识。

#### 核心目标
- 学习领域特定术语和表达
- 理解领域特定逻辑
- 保持通用能力的同时增强领域能力

#### 策略

##### 策略 A：直接领域微调
```
通用预训练模型 → 领域数据 → 领域适配模型
```

##### 策略 B：两阶段微调
```
通用预训练模型 → 领域预训练（无标注） → 任务微调（标注） → 领域任务模型
```

##### 策略 C：混合微调
```
通用预训练模型 → 混合数据（通用+领域） → 平衡模型
```

#### 适用场景
- 垂直领域（医疗、法律、金融）
- 专业知识密集型任务
- 需要领域特定术语理解

#### 注意事项
- **灾难性遗忘**：避免领域微调后丧失通用能力
- **数据平衡**：合理分配通用数据和领域数据比例
- **增量学习**：采用弹性权重整合等方法缓解遗忘

---

### 九、各类轻量微调分支

#### 1. Prefix Tuning（前缀微调）

**原理**：在输入序列前添加可学习的前缀向量，通过优化前缀向量来适应任务。

**公式**：
```
输入：[prefix] + [input]
仅优化 prefix 向量，冻结模型参数
```

**特点**：
- 不修改模型结构
- 参数极少（仅前缀向量）
- 适合自然语言生成任务

#### 2. Prompt Tuning（提示微调）

**原理**：将提示词作为可学习参数，通过优化软提示来引导模型行为。

**分类**：
- **Hard Prompt**：手动设计提示词
- **Soft Prompt**：可学习的嵌入向量

**特点**：
- 参数最少
- 灵活度高
- 效果依赖提示设计

#### 3. Adapter Tuning（适配器微调）

**原理**：在模型层间插入小型神经网络模块（Adapter），仅训练这些模块。

**结构**：
```
Transformer层 → Adapter → Transformer层
Adapter: 小型全连接网络
```

**特点**：
- 灵活可堆叠
- 可组合多个Adapter
- 适合多任务场景

#### 4. IA³（Infused Adapter by Inhibiting and Amplifying Inner Activations）

**原理**：通过学习缩放因子来调整模型内部激活值。

**公式**：
```
output = input × scaling_factor
仅优化 scaling_factor
```

**特点**：
- 参数极少
- 计算高效
- 效果接近LoRA

#### 轻量微调方法对比

| 方法 | 可训练参数 | 推理开销 | 实现复杂度 | 效果 |
|------|------------|----------|------------|------|
| **LoRA** | 低(<1%) | 无 | 低 | 优秀 |
| **Adapter** | 低(<1%) | 有 | 中 | 良好 |
| **Prefix Tuning** | 极低 | 无 | 中 | 良好 |
| **Prompt Tuning** | 极低 | 无 | 低 | 中等 |
| **IA³** | 极低 | 无 | 低 | 良好 |

---

### 十、微调工作流

#### 标准微调流程

```
1. 准备阶段
   ├── 选择基础模型
   ├── 收集与清洗数据
   └── 环境配置

2. 训练阶段
   ├── 数据格式化
   ├── 配置微调参数
   ├── 执行训练
   └── 监控训练过程

3. 评估阶段
   ├── 验证集评估
   ├── 人工评估
   └── 指标计算

4. 部署阶段
   ├── 模型合并
   ├── 模型量化
   └── 上线部署
```

#### 关键决策点

| 阶段 | 决策事项 | 推荐选择 |
|------|----------|----------|
| 模型选择 | 基础模型 | 根据任务和资源选择 |
| 微调方法 | 全量/PEFT | 资源有限选LoRA |
| 数据准备 | 数据量 | 至少数百至数千样本 |
| 训练配置 | 学习率 | 全量1e-5~5e-5，LoRA 1e-4~3e-4 |
| 训练配置 | batch size | 根据显存调整（2~8） |
| 评估 | 指标 | 根据任务选择（BLEU、ROUGE、F1等） |

---

### 十一、实践建议

#### 1. 方法选择指南

| 场景 | 推荐方法 | 理由 |
|------|----------|------|
| 资源充足 | 全量微调 | 效果最优 |
| 资源有限 | LoRA | 平衡效果与效率 |
| 多任务场景 | LoRA/Adapter | 可快速切换 |
| 领域适配 | LoRA + 领域数据 | 高效适配 |
| 对话系统 | SFT + RLHF | 对齐人类偏好 |

#### 2. 参数调优建议

| 参数 | 全量微调 | LoRA |
|------|----------|------|
| 学习率 | 1e-5 ~ 5e-5 | 1e-4 ~ 3e-4 |
| batch size | 8 ~ 32 | 2 ~ 8 |
| epoch | 3 ~ 10 | 3 ~ 5 |
| 权重衰减 | 1e-4 | 0 |

#### 3. 常见问题与解决方案

| 问题 | 原因 | 解决方案 |
|------|------|----------|
| 显存不足 | 模型过大 | 使用LoRA+量化 |
| 过拟合 | 数据不足 | 增加数据、正则化 |
| 效果不佳 | 数据质量差 | 清洗数据、增加样本 |
| 训练不稳定 | 学习率过高 | 降低学习率 |
| 灾难性遗忘 | 领域数据过多 | 混合通用数据、弹性权重 |

#### 4. 工具链推荐

| 工具 | 用途 | 特点 |
|------|------|------|
| **transformers** | 模型加载与训练 | Hugging Face 生态 |
| **peft** | PEFT方法实现 | LoRA、Adapter等 |
| **accelerate** | 分布式训练 | 简化多GPU配置 |
| **bitsandbytes** | 量化 | 4/8位量化 |
| **deepspeed** | 高效训练 | ZeRO优化 |
| **trl** | RLHF | 对齐人类偏好 |

---

### 十二、总结

**核心要点**：
1. **全量微调**：效果最优但资源需求大
2. **PEFT**：参数高效，LoRA 是当前主流
3. **SFT**：监督微调是行业标准流程
4. **指令微调**：使模型理解人类指令
5. **领域续训**：适配特定领域知识

**推荐实践**：
- **中小团队**：LoRA + SFT
- **大模型微调**：LoRA + 量化
- **对话系统**：SFT + RLHF
- **领域适配**：LoRA + 领域数据

**未来趋势**：
- PEFT 方法持续发展
- 混合微调策略（LoRA + Adapter）
- 高效训练框架优化
- 自动化微调工具
